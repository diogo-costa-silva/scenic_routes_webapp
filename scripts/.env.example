# ============================================================================
# Road Explorer Portugal - Python Scripts Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual credentials
# Command: cp .env.example .env
# ============================================================================

# ============================================================================
# Supabase Configuration
# ============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
#
# IMPORTANT: Use the SERVICE ROLE KEY (not the anon key)
# The service role key has full access to bypass RLS policies
# NEVER commit the .env file with real credentials!

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here

# ============================================================================
# Mapbox Configuration
# ============================================================================
# Get your token at: https://account.mapbox.com/access-tokens/
# This token is used for elevation data via Tilequery API
# Free tier: 100,000 requests/month

MAPBOX_TOKEN=pk.your_mapbox_public_token_here

# ============================================================================
# Optional: OpenStreetMap Overpass API
# ============================================================================
# No API key required - Overpass API is free and open
# Default endpoint: https://overpass-api.de/api/interpreter
# Rate limit: Respect fair use policy, add delays between requests

# OVERPASS_API_URL=https://overpass-api.de/api/interpreter  # Optional override

# ============================================================================
# Script Configuration (Optional)
# ============================================================================
# Customize script behavior if needed

# Enable debug logging (true/false)
# DEBUG=false

# Rate limit delay between API calls (milliseconds)
# API_DELAY_MS=100

# Sample interval for elevation queries (every Nth point)
# ELEVATION_SAMPLE_INTERVAL=10

# ============================================================================
# Security Reminders
# ============================================================================
# ✅ DO: Copy this file to .env and add real credentials
# ✅ DO: Keep .env file private and local only
# ✅ DO: Use service role key for Supabase (full access)
# ✅ DO: Commit .env.example (template) to git
# ❌ DON'T: Commit .env file with real credentials
# ❌ DON'T: Share your service role key publicly
# ❌ DON'T: Use anon key for Python scripts (insufficient permissions)
# ============================================================================
